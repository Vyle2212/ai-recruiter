import fs from "node:fs";import path from "node:path";import { buildRecruiterDashboard } from "../lib/recruiterDashboard";export function buildRecruiterDashboardAudit(){const dashboard=buildRecruiterDashboard();const has=(href:string)=>dashboard.featureCards.some(item=>item.href===href);return{generatedAt:new Date().toISOString(),mode:"read-only Recruiter Dashboard audit; no candidate DB writes; no OpenAI calls",totalCandidates:dashboard.summary.totalCandidates,readyForShortlist:dashboard.summary.readyForShortlist,needsRepair:dashboard.summary.needsRepair,featureCards:dashboard.featureCards.length,safetyPolicyPresent:Boolean(dashboard.safetyPolicy),recommendedFlowSteps:dashboard.recommendedFlow.length,milestonesListed:dashboard.milestones.length,dashboardRoute:dashboard.dashboardRoute,smartShortlistLink:has("/recruiter/smart-shortlist"),candidateCompareLink:has("/recruiter/candidate-compare"),submissionGeneratorLink:has("/recruiter/submission-generator"),clientReportLink:has("/recruiter/client-report"),importStagingLink:has("/recruiter/import-staging"),importMergeLink:has("/recruiter/import-merge"),noOpenAiCalls:true,candidateDbWrites:0,dashboard};}async function main(){const report=buildRecruiterDashboardAudit();fs.writeFileSync(path.join(process.cwd(),"reports","recruiter-dashboard-audit.json"),`${JSON.stringify(report,null,2)}\n`);console.log("Mode: read-only Recruiter Dashboard audit; no candidate DB writes; no OpenAI calls");console.log(`Total candidates: ${report.totalCandidates}`);console.log(`Ready for shortlist: ${report.readyForShortlist}`);console.log(`Needs repair: ${report.needsRepair}`);console.log(`Feature cards: ${report.featureCards}`);console.log(`Safety policy present: ${report.safetyPolicyPresent?"yes":"no"}`);console.log(`Recommended flow steps: ${report.recommendedFlowSteps}`);console.log(`Milestones listed: ${report.milestonesListed}`);console.log(`Dashboard route: ${report.dashboardRoute}`);console.log(`Smart Shortlist link: ${report.smartShortlistLink?"yes":"no"}`);console.log(`Candidate Compare link: ${report.candidateCompareLink?"yes":"no"}`);console.log(`Submission Generator link: ${report.submissionGeneratorLink?"yes":"no"}`);console.log(`Client Report link: ${report.clientReportLink?"yes":"no"}`);console.log(`Import Staging link: ${report.importStagingLink?"yes":"no"}`);console.log(`Import Merge link: ${report.importMergeLink?"yes":"no"}`);console.log("No OpenAI calls: yes");console.log("Candidate DB writes: 0");}if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditRecruiterDashboard.ts"))main().catch(error=>{console.error(error);process.exitCode=1});

