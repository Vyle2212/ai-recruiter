import fs from "node:fs";import path from "node:path";import { buildNavigationRouteAudit } from "../lib/navigationRouteAudit";export async function runUiRouteAudit(){const report=await buildNavigationRouteAudit();fs.writeFileSync(path.join(process.cwd(),"reports","ui-route-audit.json"),`${JSON.stringify(report,null,2)}\n`);return report;}async function main(){const report=await runUiRouteAudit(),sample=report.dynamicRouteSamples!;console.log("Mode: read-only UI route audit; no candidate DB writes; no OpenAI calls");console.log(`Routes checked: ${report.routesChecked}`);console.log(`Page routes checked: ${report.pageRoutesChecked}`);console.log(`API routes checked: ${report.apiRoutesChecked}`);console.log(`Files found: ${report.filesFound}`);console.log(`Missing files: ${report.missingFiles.length}`);console.log(`Broken links: ${report.brokenLinks.length}`);console.log(`Missing feature links: ${report.missingFeatureLinks.length}`);console.log(`Unsafe routes: ${report.unsafeRoutes.length}`);console.log(`Sample candidate ID: ${sample.sampleCandidateId}`);console.log(`Candidate360 sample URL: ${sample.sampleCandidate360Url}`);console.log(`Self-confirm sample URL: ${sample.sampleSelfConfirmUrl}`);console.log(`Compare sample URL generated: ${sample.sampleCompareUrl?"yes":"no"}`);console.log(`Submission sample URL generated: ${sample.sampleSubmissionUrl?"yes":"no"}`);console.log(`Client report sample URL generated: ${sample.sampleClientReportUrl?"yes":"no"}`);console.log(`Dashboard route: ${sample.sampleDashboardUrl}`);console.log(`Passed: ${report.passed?"yes":"no"}`);}if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditUiRoutes.ts"))main().catch(error=>{console.error(error);process.exitCode=1});

