import assert from "node:assert/strict";import fs from "node:fs";import {buildRecruiterAssignedJobsPreview,buildRecruiterCandidateConfirmationsPreview,buildRecruiterClientFeedbackPreview,buildRecruiterDailyWorkQueuePreview,buildRecruiterDataQualityAlertsPreview,buildRecruiterNextBestActionsPreview,buildRecruiterPortalMvpPreview,buildRecruiterPortalSummaryPreview,buildRecruiterSavedWorkPreview,buildRecruiterSubmissionsPreview,validateRecruiterPortalMvpPreview} from "../lib/recruiterPortalMvpPreview";import {buildRecruiterDashboard} from "../lib/recruiterDashboard";import {recruiterRouteRegistry} from "../lib/recruiterRouteRegistry";
const portal=buildRecruiterPortalMvpPreview();assert.equal(portal.recruiterName,"Recruiter Preview");assert.ok(buildRecruiterPortalSummaryPreview().totalCandidates>0);assert.equal(buildRecruiterDailyWorkQueuePreview().length,7);assert.ok(buildRecruiterAssignedJobsPreview().length>0);assert.ok(buildRecruiterSavedWorkPreview().savedPacks>0);assert.ok(buildRecruiterCandidateConfirmationsPreview().length>0);assert.ok(buildRecruiterSubmissionsPreview().length>0);assert.ok(buildRecruiterClientFeedbackPreview().length>0);assert.ok(buildRecruiterDataQualityAlertsPreview().length>0);assert.equal(buildRecruiterNextBestActionsPreview().length,6);assert.equal(validateRecruiterPortalMvpPreview().valid,true);assert.ok(recruiterRouteRegistry.some(item=>item.route==="/recruiter/dashboard"));assert.ok(fs.readFileSync("lib/mockAuthSessionPreview.ts","utf8").includes('getDefaultLandingRouteForRole(role)+"?role="'));const nav=fs.readFileSync("app/recruiter/layout.tsx","utf8");assert.equal(["Dashboard","Jobs","Search","Shortlist","Compare","Submissions","Reports"].filter(label=>nav.includes('"'+label+'"')).length,7);assert.equal(buildRecruiterDashboard().featureCards.length,14);assert.deepEqual(portal.safety,{candidateDbWrites:0,jobDbWrites:0,workflowWrites:0,clientDbWrites:0,feedbackWrites:0,userDbWrites:0,emailSends:0,authCalls:0,openAiCalls:0,middlewareEnforced:false});assert.equal(fs.existsSync("middleware.ts"),false);for(const file of ["lib/recruiterPortalMvpTypes.ts","lib/recruiterPortalMvpPreview.ts","app/recruiter/dashboard/page.tsx"]){const source=fs.readFileSync(file,"utf8");assert.doesNotMatch(source,/createClient\s*\(|\.auth\.|cookies\s*\(|\.insert\(|\.update\(|\.upsert\(|\.delete\(|new OpenAI|openai\.|sendMail\s*\(|sendEmail\s*\(|rmSync|unlink\s*\(/i);}console.log("recruiterPortalMvpPreview.test.ts passed");
