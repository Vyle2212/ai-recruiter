import { buildQuickFixWorkflowRefreshPreview, writeQuickFixWorkflowRefreshPreview } from "../lib/quickFixWorkflowRefreshPreview";
async function main(){const report=buildQuickFixWorkflowRefreshPreview();const outputPath=writeQuickFixWorkflowRefreshPreview(report);console.log("Mode: workflow refresh preview only; no candidate DB writes");console.log(`Candidates analyzed: ${report.candidatesAnalyzed}`);console.log(`Workflow states before: ${report.workflowStatesBefore}`);console.log(`Workflow states after preview: ${report.workflowStatesAfterPreview}`);console.log(`Would move out of needs_repair: ${report.wouldMoveOutOfNeedsRepair}`);console.log(`Would remain needs_repair: ${report.wouldRemainNeedsRepair}`);console.log(`Would become ready_for_shortlist: ${report.wouldBecomeReadyForShortlist}`);console.log(`Output path: ${outputPath}`);} if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/previewQuickFixWorkflowRefresh.ts"))main().catch(e=>{console.error(e instanceof Error?e.message:e);process.exitCode=1});
