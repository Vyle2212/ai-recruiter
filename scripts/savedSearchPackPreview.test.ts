import assert from "node:assert/strict";import fs from "node:fs";import {buildSavedPackPreview,buildSavedPackPreviewUrl,buildSavedSearchPreview,normalizeSavedPackSize} from "../lib/savedSearchPackPreview";import {buildRecruiterDashboard} from "../lib/recruiterDashboard";
const params={q:"SAP FICO",module:"FICO",country:"Malaysia",searchId:"s1",candidateIds:"a,b,a,c"};assert.equal(buildSavedSearchPreview(params).id,buildSavedSearchPreview(params).id);assert.equal(buildSavedPackPreview(params).id,buildSavedPackPreview(params).id);assert.deepEqual([5,10,20].map(normalizeSavedPackSize),[5,10,20]);assert.equal(normalizeSavedPackSize(7),5);assert.deepEqual(buildSavedPackPreview(params).candidateIds,["a","b","c"]);assert.match(buildSavedPackPreviewUrl(params),/q=SAP\+FICO/);assert.match(buildSavedPackPreviewUrl(params),/searchId=s1/);
assert.ok(fs.existsSync("app/recruiter/saved-searches/page.tsx"));assert.match(fs.readFileSync("app/recruiter/talent-search/results/page.tsx","utf8"),/buildSavedSearchPreviewUrl/);assert.match(fs.readFileSync("app/recruiter/pack-compare/page.tsx","utf8"),/buildSavedPackPreviewUrl/);assert.ok(buildRecruiterDashboard().featureCards.some(item=>item.href==="/recruiter/saved-searches"));const nav=fs.readFileSync("app/recruiter/layout.tsx","utf8");assert.equal(["Dashboard","Jobs","Search","Shortlist","Compare","Submissions","Reports"].filter(label=>nav.includes(`"${label}"`)).length,7);
for(const file of ["lib/savedSearchPackTypes.ts","lib/savedSearchPackPreview.ts","app/recruiter/saved-searches/page.tsx"]){const source=fs.readFileSync(file,"utf8");assert.doesNotMatch(source,/\.insert\(|\.update\(|\.upsert\(|\.delete\(|new OpenAI|openai\.|delete from|rmSync|unlink/i);}console.log("savedSearchPackPreview.test.ts passed");
