import assert from "node:assert/strict";import fs from "node:fs";import {buildCandidateAvailabilityPreview,buildCandidateConfirmationStatusPreview,buildCandidateConsentPreview,buildCandidateCvUploadPreview,buildCandidatePortalPreview,buildCandidateRolePreferencesPreview,buildCandidateSalaryExpectationPreview,buildCandidateWorkPreferencesPreview,validateCandidatePortalPreview} from "../lib/candidatePortalPreview";import {recruiterRouteRegistry} from "../lib/recruiterRouteRegistry";
const empty=buildCandidatePortalPreview(),linked=buildCandidatePortalPreview({candidateId:"candidate-001",preferredRoles:"SAP Consultant,SAP Consultant",workMode:"hybrid"});assert.equal(empty.candidateName,"Candidate Preview");assert.equal(linked.confirmationStatus.selfConfirmUrl,"/candidate/self-confirm/candidate-001");assert.equal(linked.confirmationStatus.recruiterReviewUrl,"/recruiter/candidate360/candidate-001");assert.ok(linked.profileCompleteness>=0);assert.equal(buildCandidateConfirmationStatusPreview({candidateId:"x"}).status,"in_progress_preview");assert.equal(buildCandidateAvailabilityPreview().previewOnly,true);assert.equal(buildCandidateSalaryExpectationPreview().previewOnly,true);assert.equal(buildCandidateRolePreferencesPreview({preferredRoles:"A,A"}).preferredRoles.length,1);assert.equal(buildCandidateWorkPreferencesPreview({workMode:"remote"}).workMode,"remote");assert.equal(buildCandidateCvUploadPreview().uploadEnabledNow,false);assert.equal(buildCandidateCvUploadPreview().storageWrites,0);assert.equal(buildCandidateConsentPreview().consentCapturedNow,false);assert.equal(validateCandidatePortalPreview().valid,true);assert.deepEqual(linked.safety,{candidateDbWrites:0,userDbWrites:0,storageWrites:0,emailSends:0,openAiCalls:0,authCalls:0,middlewareEnforced:false});assert.ok(fs.existsSync("app/candidate/portal/page.tsx"));assert.ok(recruiterRouteRegistry.some(item=>item.route==="/candidate/portal"));const selfConfirm=fs.readFileSync("app/candidate/self-confirm/[candidateId]/page.tsx","utf8"),mock=fs.readFileSync("app/auth/mock-session/page.tsx","utf8"),mockHelper=fs.readFileSync("lib/mockAuthSessionPreview.ts","utf8");assert.ok(selfConfirm.includes("/candidate/portal?candidateId="));assert.ok(mock.includes("buildMockLandingUrl"));assert.ok(mockHelper.includes("candidateId=candidate-preview"));const nav=fs.readFileSync("app/recruiter/layout.tsx","utf8");assert.equal(["Dashboard","Jobs","Search","Shortlist","Compare","Submissions","Reports"].filter(label=>nav.includes('"'+label+'"')).length,7);assert.equal(fs.existsSync("middleware.ts"),false);for(const file of ["lib/candidatePortalTypes.ts","lib/candidatePortalPreview.ts","app/candidate/portal/page.tsx"]){const source=fs.readFileSync(file,"utf8");assert.doesNotMatch(source,/createClient\s*\(|\.auth\.|cookies\s*\(|upload\s*\(|storage\.from|\.insert\(|\.update\(|\.upsert\(|\.delete\(|new OpenAI|openai\.|sendMail\s*\(|sendEmail\s*\(|rmSync|unlink\s*\(/i);}console.log("candidatePortalMvpPreview.test.ts passed");
