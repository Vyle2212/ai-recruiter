import fs from "node:fs";
import path from "node:path";
import { loadSmartShortlistBoard } from "../lib/smartShortlistData";
export async function buildSmartShortlistAudit() { const board = await loadSmartShortlistBoard(); return { generatedAt: board.generatedAt, mode: "read-only Smart Shortlist audit; no candidate DB writes", source: board.source, workflowGeneratedAt: board.workflowGeneratedAt, ...board.summary, policy: { candidateDbWrites: false, workflowWrites: false, delete: false, openAiCalls: false } }; }
async function main() { const report = await buildSmartShortlistAudit(); const output = path.join(process.cwd(), "reports", "smart-shortlist-audit.json"); fs.writeFileSync(output, `${JSON.stringify(report, null, 2)}\n`); console.log("Mode: read-only Smart Shortlist audit; no candidate DB writes"); console.log(`Total candidates: ${report.totalCandidates}`); console.log(`Ready for shortlist: ${report.readyForShortlist}`); console.log(`Cards generated: ${report.cardsGenerated}`); console.log(`High completeness: ${report.highCompleteness}`); console.log(`Medium completeness: ${report.mediumCompleteness}`); console.log(`Low completeness: ${report.lowCompleteness}`); console.log(`Missing current company: ${report.missingCurrentCompany}`); console.log(`Missing title: ${report.missingTitle}`); console.log(`Missing location: ${report.missingLocation}`); console.log(`Needs candidate confirmation: ${report.needsCandidateConfirmation}`); console.log(`Candidate360 link coverage: ${report.candidate360LinkCoverage}`); console.log(`Self-confirm preview link coverage: ${report.selfConfirmPreviewLinkCoverage}`); }
if (process.argv[1]?.replace(/\\/g, "/").endsWith("scripts/auditSmartShortlist.ts")) main().catch((error) => { console.error(error); process.exitCode = 1; });

