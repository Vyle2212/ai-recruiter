import assert from "node:assert/strict";import fs from "node:fs";import {buildAuthEnvironmentChecks,buildAuthSetupPreview,buildInviteFlowPreview,buildOwnershipPreview,buildRoleMappingPreview,getAuthImplementationRoadmap} from "../lib/authSetupPreview";
const setup=buildAuthSetupPreview();assert.equal(setup.provider.provider,"supabase");assert.equal(setup.provider.enabledNow,false);assert.equal(setup.provider.authCallsEnabled,false);assert.equal(setup.provider.middlewareEnabled,false);assert.equal(setup.environment.length,6);const marker="super-secret-service-role-value",checks=buildAuthEnvironmentChecks({SUPABASE_SERVICE_ROLE_KEY:marker});assert.equal(checks.find(item=>item.key==="SUPABASE_SERVICE_ROLE_KEY")?.status,"configured");assert.equal(JSON.stringify(checks).includes(marker),false);assert.ok(checks.every(item=>item.safeToPrint===false));assert.equal(setup.userProfileSchema.tableName,"user_profiles");assert.equal(setup.userProfileSchema.fields.length,12);assert.equal(setup.userProfileSchema.migrationStatus,"preview_only");assert.equal(buildRoleMappingPreview().length,6);assert.deepEqual(buildRoleMappingPreview().map(item=>item.role),["admin","recruiter_manager","recruiter","client","candidate","guest"]);assert.equal(buildOwnershipPreview().length,5);assert.equal(buildInviteFlowPreview().length,4);assert.ok(buildInviteFlowPreview().every(item=>item.status==="disabled_preview"&&!item.emailSendEnabled&&!item.tokenStorageEnabled));assert.equal(getAuthImplementationRoadmap().length,8);assert.ok(fs.existsSync("app/auth/setup/page.tsx"));assert.match(fs.readFileSync("app/auth/preview/page.tsx","utf8"),/\/auth\/setup/);assert.match(fs.readFileSync("lib/recruiterDashboard.ts","utf8"),/\/auth\/setup/);const nav=fs.readFileSync("app/recruiter/layout.tsx","utf8");assert.equal(["Dashboard","Jobs","Search","Shortlist","Compare","Submissions","Reports"].filter(label=>nav.includes(`"${label}"`)).length,7);assert.deepEqual(setup.safety,{noSupabaseAuthCalls:true,middlewareEnforced:false,userDbWrites:0,candidateDbWrites:0,clientDbWrites:0,emailSends:0,openAiCalls:0});for(const file of ["lib/authSetupTypes.ts","lib/authSetupPreview.ts","app/auth/setup/page.tsx"]){const source=fs.readFileSync(file,"utf8");assert.doesNotMatch(source,/createClient\s*\(|\.auth\.|\bsignIn\s*\(|\bsignUp\s*\(|\bsignOut\s*\(|inviteUser\s*\(|\.insert\(|\.update\(|\.upsert\(|\.delete\(|new OpenAI|openai\.|sendMail\s*\(|sendEmail\s*\(|rmSync\s*\(|unlink\s*\(/i);}console.log("supabaseAuthSetupPreview.test.ts passed");
