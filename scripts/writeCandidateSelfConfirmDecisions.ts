import { buildCandidateSelfConfirmDecisions } from "../lib/candidateSelfConfirmDecision";import { loadSelfConfirmStaging,writeCandidateSelfConfirmJson } from "../lib/candidateSelfConfirmFiles";
async function main(){const staging=loadSelfConfirmStaging(),file=buildCandidateSelfConfirmDecisions(staging),write=process.argv.includes("--writeDecisionFile");console.log(write?"Mode: local candidate self-confirm decision file write; no candidate DB writes":"Mode: candidate self-confirm decision preview only; no candidate DB writes");console.log(`Staging items loaded: ${staging.items.length}`);console.log(`Approve candidate confirmations: ${file.decisions.filter(item=>item.decision==="approve_candidate_confirm").length}`);console.log(`Hold for recruiter review: ${file.decisions.filter(item=>item.decision==="hold_for_recruiter_review").length}`);console.log(`Rejected candidate changes: ${file.decisions.filter(item=>item.decision==="reject_candidate_change").length}`);console.log(`Keep existing: ${file.decisions.filter(item=>item.decision==="keep_existing").length}`);if(write)console.log(`Output path: ${writeCandidateSelfConfirmJson("candidate-self-confirm-decisions.json",file)}`);}
if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/writeCandidateSelfConfirmDecisions.ts"))main().catch(error=>{console.error(error instanceof Error?error.message:error);process.exitCode=1});

