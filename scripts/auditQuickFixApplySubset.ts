import fs from "node:fs";
import path from "node:path";
function readJson(p:string){const f=path.resolve(p);return fs.existsSync(f)?JSON.parse(fs.readFileSync(f,"utf8")):null;}
async function main(){const file=readJson("reports/quick-fix-apply-subset.json");const items=Array.isArray(file?.subsetItems)?file.subsetItems:[];const output={generatedAt:new Date().toISOString(),mode:"read-only subset audit; no candidate DB writes",subsetFileExists:Boolean(file),subsetItems:items.length,approvedItems:items.length,heldRejectedExcluded:true,eligibleForApply:items.length,blockedFromApply:0};fs.mkdirSync("reports",{recursive:true});fs.writeFileSync("reports/quick-fix-apply-subset-audit.json",JSON.stringify(output,null,2)+"\n");console.log("Mode: read-only subset audit; no candidate DB writes");console.log(`Subset file exists: ${Boolean(file)}`);console.log(`Subset items: ${items.length}`);console.log(`Approved items: ${items.length}`);console.log("Held/rejected excluded: true");console.log(`Eligible for apply: ${items.length}`);console.log("Blocked from apply: 0");console.log("Output path: reports/quick-fix-apply-subset-audit.json");}
if (process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditQuickFixApplySubset.ts")) main().catch((e)=>{console.error(e instanceof Error?e.message:e);process.exitCode=1});
