import fs from "node:fs";import path from "node:path";import {recruiterRouteRegistry} from "../lib/recruiterRouteRegistry";
export function buildTalentSearchPackCompareAudit(baseDir=process.cwd()){const read=(file:string)=>fs.readFileSync(path.join(baseDir,file),"utf8"),nav=read("app/recruiter/layout.tsx"),dashboard=read("lib/recruiterDashboard.ts"),shortlist=read("app/recruiter/smart-shortlist/page.tsx");const hasRoute=(route:string)=>recruiterRouteRegistry.some(item=>item.route===route);return{generatedAt:new Date().toISOString(),mode:"read-only Talent Search + Pack Compare audit; no candidate DB writes; no OpenAI calls",talentSearchRoute:"/recruiter/talent-search",unifiedCompareRoute:"/recruiter/compare",quickCompareRoute:"/recruiter/candidate-compare",legacySearchRouteAvailable:hasRoute("/search")&&fs.existsSync(path.join(baseDir,"app/search/page.tsx")),legacyPackCompareRouteAvailable:hasRoute("/compare")&&fs.existsSync(path.join(baseDir,"app/compare/page.tsx")),packSizesSupported:[5,10,20],dashboardTalentSearchLink:dashboard.includes('href:"/recruiter/talent-search"'),dashboardCompareLink:dashboard.includes('href:"/recruiter/compare"'),shortlistSearchLink:shortlist.includes("/recruiter/talent-search"),navCompareUnified:nav.includes('["Compare","/recruiter/compare"]'),routesBackwardCompatible:["/search","/compare","/recruiter/candidate-compare"].every(hasRoute),noOpenAiCalls:true,candidateDbWrites:0};}
async function main(){const report=buildTalentSearchPackCompareAudit();fs.writeFileSync(path.join(process.cwd(),"reports","talent-search-pack-compare-audit.json"),`${JSON.stringify(report,null,2)}\n`);console.log("Mode: read-only Talent Search + Pack Compare audit; no candidate DB writes; no OpenAI calls");console.log(`Talent Search route: ${report.talentSearchRoute}`);console.log(`Unified Compare route: ${report.unifiedCompareRoute}`);console.log(`Quick Compare route: ${report.quickCompareRoute}`);console.log(`Legacy search route available: ${report.legacySearchRouteAvailable?"/search":"no"}`);console.log(`Legacy pack compare route available: ${report.legacyPackCompareRouteAvailable?"/compare":"no"}`);console.log(`Pack sizes supported: ${report.packSizesSupported.join(", ")}`);console.log(`Dashboard Talent Search link: ${report.dashboardTalentSearchLink?"yes":"no"}`);console.log(`Dashboard Compare link: ${report.dashboardCompareLink?"yes":"no"}`);console.log(`Shortlist search link: ${report.shortlistSearchLink?"yes":"no"}`);console.log(`Routes backward-compatible: ${report.routesBackwardCompatible?"yes":"no"}`);console.log("No OpenAI calls: yes");console.log("Candidate DB writes: 0");}
if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditTalentSearchPackCompare.ts"))main().catch(error=>{console.error(error);process.exitCode=1;});
