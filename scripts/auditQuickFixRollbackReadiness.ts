import { buildQuickFixRollbackReadiness, writeQuickFixRollbackReadiness } from "../lib/quickFixRollbackReadiness";
async function main(){const report=buildQuickFixRollbackReadiness();const outputPath=writeQuickFixRollbackReadiness(report);console.log("Mode: read-only rollback readiness audit; no candidate DB writes");console.log(`Backup file exists: ${report.backupFileExists}`);console.log(`Rollback file exists: ${report.rollbackFileExists}`);console.log(`Rollback items: ${report.rollbackItems}`);console.log(`Rollback coverage: ${report.rollbackCoverage}`);console.log(`Rollback safe: ${report.rollbackSafe}`);console.log(`Missing rollback items: ${report.missingRollbackItems}`);console.log(`Output path: ${outputPath}`);} if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditQuickFixRollbackReadiness.ts"))main().catch(e=>{console.error(e instanceof Error?e.message:e);process.exitCode=1});
