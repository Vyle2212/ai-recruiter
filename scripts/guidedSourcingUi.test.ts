import assert from 'node:assert/strict';import fs from 'node:fs';
const ui=fs.readFileSync('app/recruiter/talent-search/v2/GuidedSourcingPanel.tsx','utf8'),page=fs.readFileSync('app/recruiter/talent-search/v2/page.tsx','utf8'),search=fs.readFileSync('app/recruiter/talent-search/v2/CandidateSearchV2Client.tsx','utf8');
assert.match(page,/AI_GUIDED_SOURCING_PHASE1 === "true"/);assert.match(ui,/AbortController/);assert.match(ui,/latestRequest\.current/);assert.match(ui,/identityVersion\.current/);assert.match(ui,/controller\.signal\.aborted/);assert.match(ui,/buildConfirmedGuidedSearchHandoff\(confirmed\)/);assert.match(ui,/onConfirm\(handoff\)/);assert.match(ui,/sourceType=sourceMode/);assert.doesNotMatch(ui,/function reorder|Move criterion up|Move criterion down/);assert.match(ui,/generating\.current/);assert.match(ui,/binding\.contentHash===contentHash\(activeBrief\.trim\(\)\)/);assert.match(search,/function applyGuidedHandoff/);assert.match(search,/setQuery\(handoff\.query\)/);assert.doesNotMatch(search,/applyGuidedHandoff[\s\S]{0,500}runSearch\(/);console.log('guided sourcing loading, confirmation, and stale-response UI tests passed');