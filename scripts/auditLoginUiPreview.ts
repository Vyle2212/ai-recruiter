import fs from "node:fs";import path from "node:path";import {buildLoginUiPreviewState,validateLoginUiPreview} from "../lib/loginUiPreview";import {recruiterRouteRegistry} from "../lib/recruiterRouteRegistry";
export function buildLoginUiPreviewAudit(){const preview=buildLoginUiPreviewState(),validation=validateLoginUiPreview(preview),has=(route:string)=>recruiterRouteRegistry.some(item=>item.route===route);return{generatedAt:new Date().toISOString(),mode:"read-only Login UI Preview audit; no auth calls; no DB writes; no OpenAI calls",loginRoute:"/auth/login",signupRoute:"/auth/signup",forgotPasswordRoute:"/auth/forgot-password",inviteAcceptRoute:"/auth/invite",logoutRoute:"/auth/logout",roleLandingPreviewRoute:"/auth/landing-preview",loginUiPreviewGenerated:preview.routes.length===6,authFormsGenerated:preview.forms.length===5,submitButtonsDisabled:preview.forms.every(form=>!form.submitEnabled),roleLandingRoutesGenerated:preview.roleLandingRoutes.length===6,inviteTokenPrinted:preview.invitePreview.tokenPrinted,noSupabaseAuthCalls:preview.safety.supabaseAuthCalls===0,noSessionsCreated:preview.safety.sessionsCreated===0,noCookiesSet:preview.safety.cookiesSet===0,noUserDbWrites:preview.safety.userDbWrites===0,noEmailSends:preview.safety.emailSends===0,noOpenAiCalls:preview.safety.openAiCalls===0,routesBackwardCompatible:["/auth/login","/auth/signup","/auth/forgot-password","/auth/invite","/auth/logout","/auth/landing-preview","/auth/preview","/auth/setup","/auth/migrations","/auth/rls"].every(has),validation,preview};}
async function main(){const report=buildLoginUiPreviewAudit();fs.writeFileSync(path.join(process.cwd(),"reports","login-ui-preview-audit.json"),`${JSON.stringify(report,null,2)}\n`);console.log("Mode: read-only Login UI Preview audit; no auth calls; no DB writes; no OpenAI calls");console.log("Login route: /auth/login");console.log("Signup route: /auth/signup");console.log("Forgot password route: /auth/forgot-password");console.log("Invite accept route: /auth/invite");console.log("Logout route: /auth/logout");console.log("Role landing preview route: /auth/landing-preview");console.log(`Login UI preview generated: ${report.loginUiPreviewGenerated?"yes":"no"}`);console.log(`Auth forms generated: ${report.authFormsGenerated?"yes":"no"}`);console.log(`Submit buttons disabled: ${report.submitButtonsDisabled?"yes":"no"}`);console.log(`Role landing routes generated: ${report.roleLandingRoutesGenerated?"yes":"no"}`);console.log(`Invite token printed: ${report.inviteTokenPrinted?"yes":"no"}`);console.log("No Supabase Auth calls: yes");console.log("No sessions created: yes");console.log("No cookies set: yes");console.log("No user DB writes: yes");console.log("No email sends: yes");console.log("No OpenAI calls: yes");console.log(`Routes backward-compatible: ${report.routesBackwardCompatible?"yes":"no"}`);}
if(process.argv[1]?.replace(/\\/g,"/").endsWith("scripts/auditLoginUiPreview.ts"))main().catch(error=>{console.error(error);process.exitCode=1;});
