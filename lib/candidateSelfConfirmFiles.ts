import fs from "node:fs";import path from "node:path";import type { CandidateSelfConfirmDecisionFile,CandidateSelfConfirmStagingFile } from "./candidateSelfConfirmTypes";
export const CANDIDATE_SELF_CONFIRM_DIR=path.join("reports","candidate-self-confirm");
export function readCandidateSelfConfirmJson<T>(fileName:string):T|null{try{return JSON.parse(fs.readFileSync(path.resolve(CANDIDATE_SELF_CONFIRM_DIR,fileName),"utf8")) as T;}catch{return null;}}
export function writeCandidateSelfConfirmJson(fileName:string,value:unknown){const outputPath=path.resolve(CANDIDATE_SELF_CONFIRM_DIR,fileName);fs.mkdirSync(path.dirname(outputPath),{recursive:true});fs.writeFileSync(outputPath,`${JSON.stringify(value,null,2)}\n`);return outputPath;}
export function loadSelfConfirmStaging(){const staging=readCandidateSelfConfirmJson<CandidateSelfConfirmStagingFile>("candidate-self-confirm-staging.json")||readCandidateSelfConfirmJson<CandidateSelfConfirmStagingFile>("candidate-self-confirm-staging-preview.json");if(!staging)throw new Error("Missing candidate self-confirm staging preview/file.");return staging;}
export function loadSelfConfirmDecisions(staging:CandidateSelfConfirmStagingFile){return readCandidateSelfConfirmJson<CandidateSelfConfirmDecisionFile>("candidate-self-confirm-decisions.json")||{generatedAt:new Date().toISOString(),mode:"default candidate self-confirm decisions",decisions:staging.items.map(item=>({stagingItemId:item.stagingItemId,candidateId:item.candidateId,fieldName:item.fieldName,decision:item.defaultDecision,reviewerNote:"Default staging decision.",decidedAt:new Date().toISOString()}))};}

