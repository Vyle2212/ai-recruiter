# Production employment read-only refresh — 2026-09-24

The owner authorized a supervised, read-only inspection of the Supabase `ai-recruiter` project. Only `SELECT` queries were issued against that project. The staging and acceptance projects were not queried, and no database, runtime, or deployment setting was changed.

The current production inventory contains **970 candidates**, with stored employment for **231 sources / 715 rows**. A private comparison of all 970 source hashes, source `updated_at` values, and stored employment row counts against the archived September 16 review input found **960 unchanged versions and 10 changed versions**. For the ten changed versions only, the current full rows were read into an owner-only, outside-repository scratch snapshot. The other 960 rows were inherited from the archived input after the source/version/count checks. This is a **hybrid review aid, not a verified full current production backup**: unchanged employment counts do not prove unchanged employment payloads, and its snapshot must not satisfy the promotion backup or readback gate.

The current parser, evaluated against this private input at commit `2d269ef862de6d42adb2913086b9b07eaae39882`, projects **737 sources / 2,160 employment rows**, with **233 unresolved**. The unresolved groups are **125** project/client heavy, **90** heading/date boundary, **12** other narrative/layout, **4** short/missing, **1** headed table, and **1** employer label. No malformed narrative, duplicate, invalid range, or possible client-as-employer diagnostic was observed in this projection.

The owner-only review pack remains **pending**, with **533 additive review items** (510 empty-to-populated and 23 additions to existing history), **108 conflict review items**, **100 unchanged**, and **229 still empty**. It proposes **1,668 new tuples** in total; 223 existing stored tuples in conflicts would be at risk in a replacement flow. These conflicts remain blocked. The report and this checkpoint contain no candidate identifiers, contact data, source text, original filenames, or employment payloads.

Before any approval or promotion: capture and verify a **fresh full 970-row production snapshot** including the complete stored payload for each source; regenerate and review the private pack from that exact snapshot; obtain a separately verified backup/readback and release authorization. RLS cutover/readback, transactional additive backfill/search rebuild/readback, live OCR, and authenticated acceptance on the promoted artifact remain outstanding. Production remains **NO_GO**.
