import Link from "next/link";import {buildAuthSetupPreview,getAuthImplementationRoadmap,validateAuthSetupReadiness} from "../../../lib/authSetupPreview";
// Shared auth navigation links this setup page to /auth/migrations for SQL and RLS review.
// It also links to /auth/rls to review role/table ownership rules before applying policies.
const badge="rounded-full border border-slate-700 px-3 py-1",card="rounded-2xl border border-slate-800 bg-[#0B0F16] p-5";
export default function AuthSetupPage(){const setup=buildAuthSetupPreview(),readiness=validateAuthSetupReadiness(setup.environment);return <main className="min-h-screen bg-[#05070A] text-slate-100"><header className="border-b border-slate-800 bg-[#070A0F] px-6 py-8"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap gap-2 text-xs">{["Preview only","No Supabase Auth calls","No middleware","No DB writes"].map(item=><span className={badge} key={item}>{item}</span>)}</div><h1 className="mt-4 text-4xl font-semibold">Supabase Auth Setup Preview</h1><p className="mt-2 text-cyan-100">Prepare authentication, roles, and ownership before enabling login.</p><Link className="mt-4 inline-block text-sm text-cyan-300" href="/auth/preview">Back to Auth &amp; Role Access Preview →</Link></div></header><div className="mx-auto max-w-7xl space-y-8 px-6 py-8"><section className={card}><h2 className="text-lg font-semibold">Current auth status</h2><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-5"><div>Provider: Supabase</div><div>Real login enabled: No</div><div>Middleware enforced: No</div><div>Auth calls enabled: No</div><div>User DB writes: 0</div></dl><p className="mt-3 text-xs text-slate-500">Readiness: {readiness.categories.join(" · ")}</p></section><section><h2 className="mb-3 text-lg font-semibold">Environment readiness</h2><div className="overflow-auto rounded-2xl border border-slate-800"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#0B0F16] text-slate-400"><tr><th className="p-3">Environment key</th><th className="p-3">Required eventually</th><th className="p-3">Status</th><th className="p-3">Safe description</th></tr></thead><tbody>{setup.environment.map(item=><tr className="border-t border-slate-800" key={item.key}><td className="p-3 font-mono">{item.key}</td><td className="p-3">{item.requiredEventually?"Yes":"Optional / future"}</td><td className="p-3">{item.status}</td><td className="p-3 text-slate-400">{item.description} Secret values are hidden.</td></tr>)}</tbody></table></div></section><section className={card}><h2 className="text-lg font-semibold">User profile schema preview</h2><p className="mt-2 text-sm text-slate-400">Planned table: <code>user_profiles</code> · Migration status: preview only · RLS required: Yes · Not created yet</p><div className="mt-4 flex flex-wrap gap-2">{setup.userProfileSchema.fields.map(field=><span className={badge} key={field.name}>{field.name}{field.required?"":" (optional)"}</span>)}</div></section><section><h2 className="mb-3 text-lg font-semibold">Role mapping preview</h2><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{setup.roleMappings.map(item=><article className={card} key={item.role}><h3 className="font-semibold">{item.role}</h3><p className="mt-2 text-sm text-slate-400">{item.description}</p><p className="mt-3 text-xs">Landing: {item.defaultLandingRoute}</p><p className="mt-2 text-xs">Allowed: {item.allowedRouteAreas.join(", ")}</p><p className="mt-2 text-xs text-slate-500">Denied: {item.deniedRouteAreas.join(", ")||"None"}</p><p className="mt-2 text-xs text-slate-500">Organization: {item.requiresOrganization?"required":"not required"} · Client ownership: {item.requiresClientOwnership?"required":"no"} · Candidate ownership: {item.requiresCandidateOwnership?"required":"no"}</p></article>)}</div></section><section><h2 className="mb-3 text-lg font-semibold">Ownership model preview</h2><div className="grid gap-4 md:grid-cols-2">{setup.ownershipModels.map(item=><article className={card} key={item.key}><h3 className="font-semibold">{item.label}</h3><p className="mt-2 text-sm text-slate-400">{item.description}</p><p className="mt-3 text-xs">{item.organization.ownershipRule}</p><p className="mt-2 text-xs text-slate-500">Roles: {item.organization.allowedRoles.join(", ")}</p></article>)}</div></section><section><h2 className="mb-3 text-lg font-semibold">Invite flow preview</h2><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{setup.inviteFlows.map(item=><article className={card} key={item.inviteType}><h3 className="font-semibold">Invite {item.inviteType}</h3><p className="mt-2 text-sm text-amber-200">Disabled preview</p><p className="mt-2 text-xs text-slate-500">No email sending · No token storage</p></article>)}</div></section><section className={card}><h2 className="text-lg font-semibold">Implementation roadmap</h2><ol className="mt-4 grid gap-2 text-sm md:grid-cols-2">{getAuthImplementationRoadmap().map((item,index)=><li key={item}>{index+1}. {item}</li>)}</ol></section><section className="rounded-2xl border border-emerald-500/30 bg-emerald-500/5 p-5"><h2 className="font-semibold text-emerald-100">Safety</h2><p className="mt-2 text-sm text-slate-300">No Supabase Auth calls · No DB writes · No email sends · No OpenAI calls · No middleware enforcement</p></section></div></main>}
