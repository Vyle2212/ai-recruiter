import {AuthHeader,AuthLinks,card} from "../AuthUiPreview";import {buildInviteAcceptPreview} from "../../../lib/loginUiPreview";
export default async function InvitePage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams,invite=buildInviteAcceptPreview(query),email=typeof query.email==="string"?query.email:"Not provided";return <main className="min-h-screen bg-[#05070A] text-slate-100"><AuthHeader title="Accept invite" subtitle="Preview invite acceptance without validating or storing credentials."/><div className="mx-auto max-w-3xl space-y-6 px-6 py-8"><section className={card}><h2 className="font-semibold">Invite preview summary</h2><dl className="mt-4 space-y-3 text-sm"><div>Invite type: {invite.inviteType}</div><div>Invited role: {invite.invitedRole}</div><div>Email: {email}</div><div>Token status: hidden / not printed</div></dl><button disabled className="mt-5 w-full cursor-not-allowed rounded-lg bg-slate-700 px-4 py-3 font-semibold text-slate-400">Accept invite — coming soon</button><p className="mt-3 text-xs text-slate-500">No token validated · No account created · No email sent.</p></section><AuthLinks/></div></main>}
