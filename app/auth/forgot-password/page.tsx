import {AuthHeader,DisabledForm,AuthLinks} from "../AuthUiPreview";import {buildAuthFormPreview} from "../../../lib/loginUiPreview";
export default function ForgotPasswordPage(){return <main className="min-h-screen bg-[#05070A] text-slate-100"><AuthHeader title="Forgot password" subtitle="Preview password recovery before authentication is enabled."/><div className="mx-auto max-w-3xl space-y-6 px-6 py-8"><DisabledForm form={buildAuthFormPreview("forgot_password")} buttonLabel="Send reset link — coming soon"/><p className="text-sm text-slate-400">No reset email sent · Supabase Auth is not enabled yet.</p><AuthLinks/></div></main>}
