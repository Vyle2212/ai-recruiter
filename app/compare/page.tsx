import {CandidateCompareWorkspace} from "@/components/candidate-compare-workspace";
import Link from "next/link";
export default async function ComparePage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams,forward=new URLSearchParams();for(const[key,value]of Object.entries(query)){if(typeof value==="string")forward.set(key,value);else for(const item of value||[])forward.append(key,item);}const suffix=forward.size?`?${forward.toString()}`:"";return <><div className="border-b border-violet-500/20 bg-[#0B0F16] px-5 py-3 text-white"><div className="mx-auto flex max-w-[1500px] flex-wrap items-center justify-between gap-3"><div><span className="text-xs font-semibold uppercase text-violet-200">Using legacy Pack Compare view</span><p className="text-sm text-slate-400">Executive comparison for Top 5, Top 10, or Top 20 Pack Compare modes.</p></div><div className="flex flex-wrap gap-3 text-sm"><Link href={`/recruiter/pack-compare${suffix}`} className="text-violet-100">Open in Recruiter Pack Compare</Link><Link href={`/recruiter/compare${suffix}`} className="text-cyan-100">Back to Recruiter Compare</Link><Link href={`/recruiter/talent-search/results${suffix}`} className="text-cyan-100">Back to Talent Search Results</Link></div></div></div><CandidateCompareWorkspace/></>}
