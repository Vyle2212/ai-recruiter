"use client";
import Link from "next/link";
import {useState} from "react";
const field="rounded-lg border border-slate-700 bg-[#05070A] px-3 py-2.5 text-sm outline-none focus:border-cyan-400";
const samples=[["SAP FICO Malaysia","SAP FICO","Malaysia"],["SAP BTP Malaysia","SAP BTP","Malaysia"],["SAP MM Vietnam","SAP MM","Vietnam"],["SAP ABAP Philippines","SAP ABAP","Philippines"]] as const;
export default function TalentSearchBridge(){const[form,setForm]=useState({keyword:"",modules:"",country:"",location:"",years:"",company:""});const set=(key:string,value:string)=>setForm(current=>({...current,[key]:value}));const params=new URLSearchParams();if(form.keyword)params.set("q",form.keyword);if(form.modules)params.set("module",form.modules);if(form.country)params.set("country",form.country);if(form.location)params.set("location",form.location);if(form.years)params.set("minYears",form.years);if(form.company)params.set("company",form.company);const href=`/search?${params.toString()}`;return <main className="min-h-screen bg-[#05070A] text-slate-100"><header className="border-b border-slate-800 bg-[#070A0F] px-6 py-8"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap gap-2"><span className="rounded-full border border-emerald-500/30 px-3 py-1 text-xs text-emerald-100">Read-only</span><span className="rounded-full border border-slate-700 px-3 py-1 text-xs">No DB writes</span><span className="rounded-full border border-cyan-500/30 px-3 py-1 text-xs text-cyan-100">Deterministic v1</span></div><h1 className="mt-4 text-4xl font-semibold">Talent Search</h1><p className="mt-2 text-lg text-cyan-100">Search the talent pool and build shortlist packs.</p></div></header><section className="mx-auto max-w-6xl space-y-6 px-6 py-8"><div className="grid gap-3 md:grid-cols-5">{["Search by role, skill, or location","Review matched candidates","Build Pack 5 / 10 / 20","Compare the pack","Generate submissions and reports"].map((item,index)=><div key={item} className="rounded-xl border border-slate-800 bg-[#0B0F16] p-4"><span className="text-xs font-semibold text-cyan-200">Step {index+1}</span><p className="mt-2 text-sm">{item}</p></div>)}</div><div className="rounded-2xl border border-slate-800 bg-[#0B0F16] p-6"><h2 className="text-lg font-semibold">Search criteria</h2><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3"><input className={field} placeholder="Role or keyword" value={form.keyword} onChange={e=>set("keyword",e.target.value)}/><input className={field} placeholder="SAP skills / modules" value={form.modules} onChange={e=>set("modules",e.target.value)}/><input className={field} placeholder="Country" value={form.country} onChange={e=>set("country",e.target.value)}/><input className={field} placeholder="City / location" value={form.location} onChange={e=>set("location",e.target.value)}/><input className={field} type="number" min="0" placeholder="Minimum years experience" value={form.years} onChange={e=>set("years",e.target.value)}/><input className={field} placeholder="Company / background (optional)" value={form.company} onChange={e=>set("company",e.target.value)}/></div><Link href={href} className="mt-5 inline-flex rounded-lg bg-cyan-400 px-5 py-3 text-sm font-semibold text-slate-950">Search Talent Pool</Link><p className="mt-3 text-xs text-slate-500">Results open in the existing read-only Talent Search experience.</p></div><div><h2 className="font-semibold">Sample searches</h2><div className="mt-3 flex flex-wrap gap-2">{samples.map(([label,module,country])=><Link key={label} href={`/search?q=${encodeURIComponent(module)}&module=${encodeURIComponent(module)}&country=${encodeURIComponent(country)}`} className="rounded-full border border-cyan-500/30 px-4 py-2 text-sm text-cyan-100">{label}</Link>)}</div></div></section></main>}
