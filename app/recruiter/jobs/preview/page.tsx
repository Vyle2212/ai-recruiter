import Link from "next/link";import {buildJobClientReportUrl,buildJobPackCompareUrl,buildJobPreview,buildJobSavedSearchUrl,buildJobTalentSearchUrl} from "../../../../lib/jobPreview";
const action="inline-flex rounded-lg border border-slate-700 px-4 py-2 text-sm hover:border-cyan-400";
export default async function JobPreviewPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const params=await searchParams,job=buildJobPreview(params),ids=(Array.isArray(params.candidateIds)?params.candidateIds:[params.candidateIds]).filter((item):item is string=>Boolean(item)).flatMap(item=>item.split(",")).filter(Boolean),candidateId=(Array.isArray(params.candidateId)?params.candidateId[0]:params.candidateId)||ids[0],reportUrl=ids.length?buildJobClientReportUrl(params,ids):undefined,submissionQuery=new URLSearchParams();if(candidateId)submissionQuery.set("candidateId",candidateId);submissionQuery.set("jobId",job.id);submissionQuery.set("jobTitle",job.jobTitle);submissionQuery.set("clientName",job.clientName);const requirements=[...[...job.mustHaveSkills].map(value=>["Must have",value]),...[...job.niceToHaveSkills].map(value=>["Nice to have",value]),...[...job.modules].map(value=>["Module / stack",value]),...[...job.targetCompanies].map(value=>["Target company",value])];return <main className="min-h-screen bg-[#05070A] text-slate-100"><header className="border-b border-slate-800 bg-[#070A0F] px-6 py-8"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full border border-cyan-500/30 px-3 py-1 text-xs">Job Preview</span><span className="rounded-full border border-amber-500/30 px-3 py-1 text-xs text-amber-100">{job.status}</span></div><h1 className="mt-4 text-4xl font-semibold">{job.jobTitle}</h1><p className="mt-2 text-cyan-100">{job.clientName}</p><Link href="/recruiter/jobs" className="mt-4 inline-flex text-sm text-slate-400">Back to Jobs</Link></div></header><div className="mx-auto max-w-7xl space-y-6 px-6 py-8">
<section className="rounded-2xl border border-slate-800 bg-[#0B0F16] p-6"><h2 className="text-lg font-semibold">Job Summary</h2><dl className="mt-4 grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-4">{[["Title",job.jobTitle],["Client",job.clientName],["Location",[job.city,job.country].filter(Boolean).join(", ")||"Not provided"],["Work mode",job.workMode||"unknown"],["Employment",job.employmentType||"Not provided"],["Seniority",job.seniority||"Not provided"],["Priority",job.priority||"medium"],["Salary / rate",job.salaryMin!==undefined||job.salaryMax!==undefined?`${job.currency||""} ${job.salaryMin??"—"} – ${job.salaryMax??"—"}`:"Not provided"],["Contract duration",job.contractDuration||"Not provided"]].map(([label,value])=><div key={label}><dt className="text-slate-500">{label}</dt><dd className="mt-1">{value}</dd></div>)}</dl></section>
<section className="rounded-2xl border border-slate-800 bg-[#0B0F16] p-6"><h2 className="text-lg font-semibold">Requirements</h2>{requirements.length?<div className="mt-4 flex flex-wrap gap-2">{requirements.map(([label,value])=><span key={`${label}-${value}`} className="rounded-full border border-slate-700 px-3 py-1 text-xs"><span className="text-slate-500">{label}:</span> {value}</span>)}</div>:<p className="mt-3 text-sm text-slate-400">No structured requirements provided.</p>}{job.jdText&&<div className="mt-5"><h3 className="text-sm font-semibold">Job description</h3><p className="mt-2 whitespace-pre-wrap text-sm text-slate-300">{job.jdText}</p></div>}{job.notes&&<div className="mt-5"><h3 className="text-sm font-semibold">Notes</h3><p className="mt-2 whitespace-pre-wrap text-sm text-slate-300">{job.notes}</p></div>}</section>
<section className="rounded-2xl border border-cyan-500/30 bg-cyan-500/5 p-6"><h2 className="text-lg font-semibold">Recruiter actions</h2><div className="mt-4 flex flex-wrap gap-3"><Link href={buildJobTalentSearchUrl(params)} className="rounded-lg bg-cyan-400 px-4 py-2 text-sm font-semibold text-slate-950">Search Talent Pool for this job</Link>{[5,10,20].map(size=><Link key={size} href={buildJobPackCompareUrl(params,size)} className={action}>Build Pack {size}</Link>)}<Link href={buildJobSavedSearchUrl(params)} className={action}>Save Search Preview</Link>{reportUrl?<Link href={reportUrl} className={action}>Create Client Report Preview</Link>:<span aria-disabled="true" className={`${action} cursor-not-allowed text-slate-500`}>Client Report — candidateIds required</span>}{candidateId?<Link href={`/recruiter/submission-generator?${submissionQuery.toString()}`} className={action}>Submission draft</Link>:<span aria-disabled="true" className={`${action} cursor-not-allowed text-slate-500`}>Submission draft — candidateId required</span>}</div></section>
<section className="rounded-2xl border border-violet-500/20 bg-violet-500/5 p-6"><h2 className="font-semibold">Job workflow preview</h2><p className="mt-3 text-sm text-slate-300">Job → Search → Pack → Compare → Shortlist → Submission → Report → Client Feedback</p></section><section className="rounded-2xl border border-amber-500/30 bg-amber-500/5 p-6"><h2 className="font-semibold text-amber-100">Safety and limitations</h2><p className="mt-2 text-sm text-slate-300">Preview-only. No database persistence, candidate database writes, job database writes, or OpenAI calls.</p></section>
</div></main>}
