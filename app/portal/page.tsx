import Link from "next/link";import {buildPortalNavigationPreview} from "../../lib/portalNavigationPreview";
const panel="rounded-2xl border border-slate-800 bg-[#0B0F16] p-5",badge="rounded-full border border-slate-700 px-3 py-1",link="text-cyan-300 hover:text-cyan-200";
export default function PortalPage(){const preview=buildPortalNavigationPreview();return <main className="min-h-screen bg-[#05070A] text-slate-100"><header className="border-b border-slate-800 bg-[#070A0F] px-6 py-10"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap gap-2 text-xs">{["Preview only","No real login","No DB writes","No middleware"].map(item=><span className={badge} key={item}>{item}</span>)}</div><h1 className="mt-5 text-4xl font-semibold">AI Primus Portal</h1><p className="mt-2 text-cyan-100">Choose the right workspace for recruiter, client, candidate, or admin users.</p></div></header><div className="mx-auto max-w-7xl space-y-8 px-6 py-8"><section><h2 className="text-xl font-semibold">Portal chooser</h2><div className="mt-4 grid gap-5 lg:grid-cols-2">{preview.portals.map(portal=><article className={panel} key={portal.key}><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="text-xl font-semibold">{portal.label}</h3><p className="mt-1 text-xs text-slate-500">{portal.route}</p></div><span className={badge+" text-xs"}>Preview only</span></div><p className="mt-4 text-sm text-slate-300">{portal.description}</p><p className="mt-3 text-xs text-slate-500">Intended roles: {portal.intendedRoles.join(", ")}</p><div className="mt-4 flex flex-wrap gap-2">{portal.capabilities.map(item=><span className="rounded-lg bg-slate-900 px-3 py-1 text-xs text-slate-300" key={item}>{item}</span>)}</div><div className="mt-5 flex flex-wrap gap-4 text-sm"><Link className={link} href={portal.route}>Open {portal.label}</Link><Link className={link} href={portal.mockSessionRoute}>Mock as {portal.key}</Link><Link className={link} href={portal.authPreviewRoute}>Login preview</Link></div><p className="mt-4 text-xs text-amber-100">{portal.safetyNotes.join(" ")}</p></article>)}</div></section><section className={panel}><h2 className="font-semibold">Role entry preview</h2><div className="mt-4 grid gap-3 md:grid-cols-2 lg:grid-cols-3">{preview.roleEntryPoints.map(entry=><div className="rounded-xl border border-slate-800 p-4" key={entry.role}><p className="font-medium">{entry.role}</p><p className="mt-1 text-sm text-slate-400">{entry.portalKey} → {entry.defaultRoute}</p><div className="mt-3 flex gap-3 text-xs"><Link className={link} href={entry.loginPreviewRoute}>Login</Link><Link className={link} href={entry.mockSessionRoute}>Mock session</Link></div></div>)}</div></section><section><h2 className="text-xl font-semibold">Recommended user flows</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{preview.recommendedFlows.map(flow=><article className={panel} key={flow.flowKey}><h3 className="font-semibold">{flow.label}</h3><ol className="mt-4 space-y-2 text-sm text-slate-300">{flow.steps.map((step,index)=><li key={step}><span className="mr-2 text-cyan-300">{index+1}.</span>{step}</li>)}</ol></article>)}</div></section><section className={panel}><h2 className="font-semibold">Auth preview links</h2><div className="mt-4 flex flex-wrap gap-4 text-sm">{[["Login UI Preview","/auth/login"],["Mock Session Preview","/auth/mock-session"],["Role Access Preview","/auth/preview"],["Middleware Guard Preview","/auth/middleware"],["Staging Auth Plan","/auth/staging"]].map(([label,href])=><Link className={link} href={href} key={href}>{label}</Link>)}</div></section><section className="rounded-2xl border border-emerald-500/30 bg-emerald-500/5 p-5"><h2 className="font-semibold text-emerald-100">Safety</h2><p className="mt-2 text-sm text-slate-300">No real auth · No DB or workflow writes · No email sends · No OpenAI calls · Preview-only</p></section></div></main>}
