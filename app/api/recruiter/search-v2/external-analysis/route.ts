import { NextRequest,NextResponse } from "next/server";
import { generateExternalTalentAnalysis } from "@/lib/externalTalentAnalysis";
import { ExternalSourceError } from "@/lib/externalCandidateSourceProvider";
export const runtime="nodejs";export async function POST(request:NextRequest){const started=performance.now();try{const body=await request.json();if(!body||typeof body!=="object"||typeof body.candidateId!=="string"||!Array.isArray(body.evidence))return NextResponse.json({error:"Invalid analysis request."},{status:400});const analysis=await generateExternalTalentAnalysis({candidateId:body.candidateId,headline:body.headline,location:body.location,employer:body.employer,evidence:body.evidence.slice(0,12).map((item:unknown)=>{const value=item&&typeof item==="object"?item as Record<string,unknown>:{};return{label:String(value.label||"Evidence"),excerpt:String(value.excerpt||"").slice(0,500)}})},request.signal);return NextResponse.json({analysis,timing:{optionalClaudeMs:performance.now()-started}},{headers:{"Cache-Control":"no-store"}});}catch(error){const failure=error instanceof ExternalSourceError?error:new ExternalSourceError("PROVIDER_ERROR","AI Match Analysis is temporarily unavailable.");return NextResponse.json({error:failure.message,reason:failure.code},{status:failure.code==="RATE_LIMITED"?429:503});}}
